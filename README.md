# EventEmitter
Basic javascript EventEmitter utility class. This class makes it easy for separate components to talk to each other or even pass data around. It's easier to grasp than redux, however probably shouldn't be used in place of redux or flux for large, complicated react projects.
